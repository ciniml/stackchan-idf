- **近接センサーで反応** (CoreS3 のみ): 画面上辺の内蔵センサーに手を近づけると、うれしい顔になって近接フレーズを話します (フレーズは音声タブの「近接時の発話フレーズ」で設定、空なら吹き出しのみ)。離すと表情が戻ります。会話中は反応しません。設定ページの「会話」タブ →「近接センサー」で ON/OFF、near / far の閾値、判定の連続時間、反応の間隔を変えられます (すべて即時反映・再起動不要)
- **近接センサーの現在値をリアルタイム表示**: BLE 接続すると自動で 0.5 秒ごとに更新され、直近 30 秒のグラフ (near / far の線入り) が出ます。「今の値を near にする」で閾値を決められます
- **センサー調整モード**: 受光ゲイン (x16 / x32 / x64)、LED 電流・duty・周波数、パルス数、測定周期、オフセット (クロストーク相殺) を変えて「センサーに適用」。既定は x64 / 100 mA / 15 パルス / 50 ms、閾値 near 335 / far 240
- リリースノートの構成を見直しました (このリリースから、そのリリースの主要変更点を中心に記載)

このアルファで確認したいこと: 手を近づけたときの反応の出やすさ (誤反応 / 無反応)、既定の閾値が手元の機体で合うか (合わなければ調整モードとリアルタイム表示で決めた値を教えてください)。
